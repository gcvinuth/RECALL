{
  "name": "recall",
  "version": "1.0.0",
  "description": "Recall - Personal OS for Relationships",
  "scripts": {
    "build": "mkdir -p www && cp index.html app.js style.css manifest.json sw.js www/ && cp -r modules www/ || true"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
